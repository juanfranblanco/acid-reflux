require('./LoadingSpinner.css');

import React from 'react/addons';
export default React.createClass({
  render() {
    return (
      <div className='loadingspinner'><div></div></div>
    );
  }
});
